`otter unlock` — unlock a container to allow removal and upgrades.

## Synopsis

```text
otter ulk    [options] [container...]
otter unlock [options] [container...]
```

`ulk` is a full alias of `unlock`; the two behave identically.

## Description

Removes the lock file (`/usr/lib/otter/container.lock`) from the named
container(s), or every otter container with `--all`, reversing
`otter lock`. The container must be running for `unlock` to remove the
file. Containers that aren't currently locked are skipped without
being treated as a failure.

## Options

| Flag     | Alias |
| -------- | ----- |
| `--all`  | `-a`  |
| `--root` | `-r`  |

## Options Explained

### `--all`, `-a`

Unlock every otter container.

### `--root`, `-r`

Unlock a rootful container.

## Examples

```sh
otter ulk my-box
```

Unlocks `my-box`.

```sh
otter unlock my-box
```

Same as above, using the non-aliased command name.

```sh
otter ulk my-box --root
```

Unlocks the rootful container `my-box`.

```sh
otter unlock ubuntu,fedora --all
```

Unlocks every otter container regardless of the positional names given —
`--all` takes precedence over any explicit names.

## Notes

- The container must be running to unlock — use `otter start` first if
  needed.
- Already-unlocked containers are skipped, not treated as an error.

## See Also

- otter-lock — locks a container to protect it from
  removal/upgrade.
